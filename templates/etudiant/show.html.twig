{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="card p-5">
            <div class="card-body">
                <h1>Etudiant</h1>
                <table class="table">
                    <tbody>
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>Prenom</th>
                        <th>Formation</th>
                        <th>Annee dans la formation</th>
                    </tr>
                    </thead>
                    <tr>
                        <td>{{ etudiant.id }}</td>
                        <td>{{ etudiant.RFID.nom }}</td>
                        <td>{{ etudiant.RFID.prenom }}</td>
                        <td>{{ etudiant.promotion.formation.diplome }}</td>
                        <td>{{ etudiant.promotion.anneeFormation }}</td>
                    </tr>
                    </tbody>
                </table>

                <a class="btn btn-primary" href="{{ path('etudiant_index') }}">Retour à la liste</a>

                <a class="btn btn-primary" href="{{ path('etudiant_edit', {'id': etudiant.id}) }}">Modifier</a>

                {{ include('etudiant/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
