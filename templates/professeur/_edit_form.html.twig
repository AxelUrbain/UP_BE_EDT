{{ form_start(form) }}
<div class="container">
    {{ form_row(form.statut , {'label':'Statut'}) }}
    {{ form_row(form.specialite, {'label':'Spécialité'}) }}
</div>
<div class="row">
    <a class="btn btn-outline-info" href="{{ path('professeur_index') }}">retour</a>
    <button class="btn btn-outline-primary">Modifier</button>
</div>
{{ form_end(form) }}